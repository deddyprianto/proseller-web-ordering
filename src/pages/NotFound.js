import React from "react";

function NotFound() {
  return (
    <div
      style={{
        height: "100vh",
        width: "100vw",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        padding: "3rem",
      }}
    >
      <h1>Sorry, page not found in this server :(</h1>
    </div>
  );
}

export default NotFound;
